#include <stdlib.h>
#include <stdio.h>

/**
 * main - returns an integer or
 * argument string
 * @argc: the argument count
 * @argv: the argument vector which is
 * array of strings
 * Return: argv
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);


	return (0);
}
