#include "main.h"
#include <stdio.h>

/**
 * mul - it multiplies two integers
 * @a: the number to multiply
 * @b: the number to be multiplied with
 * Return: The multiple of the numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
